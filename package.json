{
  "name": "lib",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "rollup --config",
    "clean": "rm -rf dist.js node_modules .rollup.cache",
    "demo": "npm run initialize && npm run build && npm run run",
    "initialize": "npm run clean && npm ci",
    "run": "node dist.js"
  },
  "dependencies": {
    "lib0": "^0.2.85"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.1",
    "rollup": "^3.29.2"
  }
}
